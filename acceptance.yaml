# acceptance.yaml — acceptance spec for TeaPrompt (read-only by convention)
# Generated by verification-map-generator, 2026-09-23; route-evals check widened
# to all three fixtures 2026-09-24 (source_commit 7d8de92).
# Oracle rule: a spec-vs-repo mismatch is a spec-oracle error or a product
# regression — never edit this file to make a run pass. Git does not preserve
# file modes, so the read-only status is a convention, not a mechanism; edits
# trigger `make all` via .pre-commit-config.yaml and need human review.
locked: true
checks:
  - id: core-skill-cardinality
    description: CORE_SKILLS in plans/validate_skill_examples.py lists exactly 9 skills
    verify: python3 -c "import re; s=open('reflective-prompt-library/plans/validate_skill_examples.py').read(); print(len(re.findall(r'\"([a-z-]+)\"', s.split('CORE_SKILLS = [')[1].split(']')[0])))"
    expect: "9"
  - id: domain-pack-cardinality
    description: DOMAIN_PACK_SKILLS lists exactly 5 packs
    verify: python3 -c "import re; s=open('reflective-prompt-library/plans/validate_skill_examples.py').read(); print(len(re.findall(r'\"([a-z-]+)\"', s.split('DOMAIN_PACK_SKILLS = [')[1].split(']')[0])))"
    expect: "5"
  - id: gate-suite-green
    description: make all exits 0
    verify: make all
    expect_exit: 0
  - id: test-suite-green
    description: governance pytest suite has zero failures
    verify: python3 -m pytest reflective-prompt-library/plans/tests/ -q
    expect_exit: 0
  - id: route-evals-green
    description: all three route fixtures pass Phase-1 threshold
    verify: python3 reflective-prompt-library/plans/route_paraphrase_eval.py reflective-prompt-library/plans/route-001-paraphrase-eval.yaml && python3 reflective-prompt-library/plans/route_paraphrase_eval.py reflective-prompt-library/plans/route-002-holdout-eval.yaml && python3 reflective-prompt-library/plans/route_paraphrase_eval.py reflective-prompt-library/plans/route-003-adversarial-eval.yaml
    expect_exit: 0
