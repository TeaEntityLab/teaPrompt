# Governance, Workflow, and Self-Control — User-Directed Adoption Pass — 2026-09-14

> **Status: decided (non-authoritative); user-directed adoption record.** User instruction: *"Implement governance and workflow and self-control parts as possible if worth it."* Scope: the governance / workflow / self-control items left open by the [September review](september-skills-review-2026-09-14.md), the [GD-18 review](governed-delivery-review-2026-09-13.md), and the agentflow records. Bar applied per item: the installed surface must be better; name the failure defended, the smaller alternative rejected, and the gap; one surface; ≤2 sentences of prose; ledger row + guard. Date-gated items and named-trigger reserves were not preempted — a generic "if worth it" does not fire a named gate. Authority chain unchanged: `06-repo/AGENTS.md` and the invoked `SKILL.md` contracts govern.

## Direct Answer

Seven items were worth it and landed (plus one template defect fixed by the second pass); one was rejected on evidence; six were evaluated and held, each with its reason. The largest is workflow-routing governance: the deferred GD-19 collision measurement ran, found one real derailment, and the router was tuned under holdout-before-tune with all three evals at 100%. The rest are self-control mechanisms for the repository and its agents: a guard that fails when the discovery index is stale, a "search before you add" rule on the skill that owns minimality, a status-family map for cross-phase handoffs, and evidence surfaces the 2026-10-11 checkpoint needs.

## Candidate Adoption Ledger

| # | Candidate | Status | Evidence | Next action / trigger |
| --- | --- | --- | --- | --- |
| GW-1 | GD-19 collision measurement for "deliver / autonomous / unattended" and the resulting R11 tune | **Adopted 2026-09-14** (two rounds) | Round 1: nine fresh ROUTE-002 phrases (3 groups: plan / route / risk) passed 100% pre-tune; ROUTE-003 trap `delivery_vocab_implement_not_plan_trap` held 33.3% pre-tune; tuned by the marker `approved delivery plan` + verification context. **Round 1 was not the narrowest fix and not fully disclosed:** the bare token `verify` pulled review/risk/test-plan requests about an approved spec into implement (three regressions found by the second-pass routing lens), and two failing probe phrases (a skill-vs-pack selection question → spec-plan; a bare `deploy` / `money` hazard → dispatch) were replaced instead of fixture-backed, only one of them recorded. Round 2 (second pass): every shape fixture-backed first — `approved_spec_verify_not_implement_trap` 0%, `delivery_vocab_route_not_plan_trap` 80%, `deploy_money_risk_trap` 0%, zh-TW implement phrase 75% pre-tune — then tuned narrowly: `verify` → `gate passes`; `skill` / `pack` / `reflective-` as selection targets; `which skill should` as a catalog signal that R11 defers to; bare `deploy` and `money` as risk keywords (a pre-existing gap against the AGENTS.md Human Review list); three zh-TW approved-delivery-plan markers. Two of my draft trap phrases were replaced with the reason recorded: they carried R9 planning signals (`acceptance criteria`, `before implementation`) so their expected label contradicted the contract. Post-tune ROUTE-001/002/003 = 100% (16 / 47 / 26 groups); floors 47/134 and 26/91; R11 contract bullet; 13 `GD19_*` probes | GD-19 row closed in the GD adoption ledger; no quick-cue added (R12: not yet frequent) |
| GW-2 | Index-currency guard: committed `index.json` must equal an in-memory generator run | **Adopted 2026-09-14** (strengthened by the second pass) | `test_index_json_current.py` compares whole entries plus the category file lists (the first version compared only line counts, so a same-line-count edit to a description or heading escaped it — second-pass guards lens); failed on the live tree for the exact files edited, including a one-line wording change (observed); never writes | Failure defended: index went stale twice in one session (2026-09-13/14); smaller alternative rejected: running the generator inside `make validate` writes a committed artifact during validation |
| GW-3 | Ledger Status Families map in GLOSSARY | **Adopted 2026-09-14** (corrected by the second pass) | One table translating `open` / `unverified` / `pending` / `asserted` and siblings across the four ledgers; `needs-qualification` was missing and the claim "each set is pinned by its skill's guard" was false for research and review — both now pinned in this record's guard | Smaller alternative to vocabulary unification (C18), which stays trigger-gated |
| GW-4 | Search-before-add rule on `reflective-minimality` (prompt-text bullet) | **Adopted 2026-09-14** (trimmed by the second pass) | One sentence appended to the OG-1 bullet: search the surface for a sentence already carrying the clause and extend it; a trailing repo-history clause ("two same-day adoptions … is how twins arrive") was stripped as non-portable narration; guard: OG-1 pin unchanged (count 1) + this record's guard | Failure defended: three same-day twins on 2026-09-03 (September review C1–C3); smaller alternative rejected: the roadmap's Horizon 1 duty is invisible to an installed agent |
| GW-5 | `reflective-implement` Before Editing step 4 compacted from six bullets to one self-contained sentence | **Adopted 2026-09-14** (repaired by the second pass) | The first compaction claimed "same clauses" but dropped `Socratic questions`, `falsifiability`, `official`, `project`, and `to an existing contract`; all restored — one sentence, every original clause; steps renumbered 4–8; no cross-reference needed, so the C16 policy question dissolves | — |
| GW-6 | Pack examples for the four uncovered templates (writer-critic with floor, multi-wave; orchestrator-workers, DAG) | **Adopted 2026-09-14** (rig-run and corrected by the second pass) | `flow-loop-harness.examples.md` Examples 3–4; `flow-control-generator.examples.md` Examples 4–5. First drafts were written from reading the templates; the second pass ran all four through stub rigs (26 exit paths observed) and found two claims the templates did not implement — Example 4's worker confinement ("read-only plus a scratch dir": a host property, replaced by the template's task-text-is-data rule) and Example 5's "per-node gate on output presence" (missing: see GW-15). Every Verification line now states the observed rig results with the run date | C19 closed; the conditional router stays uncovered |
| GW-7 | Usage log widened to all four packs; 2026-09-05 template-maintenance note backfilled | **Adopted 2026-09-14** | `flow-pack-usage-log.md`: zero-state rows for `agent-governance-scaffold` and `governed-delivery`, `unknown`-not-zero wording, checkpoint pointer; `test_usage_log_evidence_contract` green | The 2026-10-11 GD checkpoint (runbook check 8 / Agenda 7) reads this row |
| GW-8 | Intra-skill near-duplicate guard (token-set Jaccard over sentences) | **Rejected on evidence 2026-09-14** | Prototype at threshold 0.6 over ≥10 content tokens, run on `4d90c68` (pre-merge) and HEAD: missed all three 2026-09-03 twins (semantic duplicates with different tokens) and raised six flags in three by-design classes (frontmatter description ≈ Trigger in three skills; GD template ≈ Methods; GDR-6 ≈ footer) | Re-open only with a detector that catches the three known twins without flagging the known restatements |
| GW-9 | Flow-pack lint tier (25k pack tier vs template factoring) | **Held — date-gated** | Both packs ≤ 20k with < 1k headroom; Horizon 1 zero-sum rule in force | 2026-10-11 runbook Agenda 6/7 |
| GW-10 | A1 anchor tightening (`reflective-brief`) | **Held — date-gated** | E1 already merged (September review C3); A1 keeps its owner/unknown-unknowns clause until the checkpoint weighs it | 2026-10-11 runbook Agenda 7 |
| GW-11 | Ledger status vocabulary unification | **Held — trigger unfired** | GW-3 map adopted instead; each set is pinned by its skill's guard | A documented cross-skill handoff confusion |
| GW-12 | I-1 / A-5, TK-1, E-5 reserved sentences | **Held — named gates** | Each record names its own local-case trigger; I-1 carries an absence guard; AF82-8 affirms only the original triggers reopen them | Their own triggers |
| GW-13 | GD-16 refuters host run; GA-13 fault-injection suite | **Held — host-only** | No named host harness; TeaPrompt CI runs no providers | A named host harness |
| GW-14 | agentflow record-only / held rows (AF-1, AF-5, AF-8, AF-13; CX-7–CX-20; TK-2–TK-6; AF82-3/5/10/11) | **Held — panel-decided** | Each row already carries a 7/7 or coordinator disposition with reasons and a reopen trigger; no new local evidence arrived | Their recorded triggers |
| GW-15 | Python pack templates accept a zero-byte success: a node or worker exiting 0 with no output was marked done and fed empty text downstream (the bash templates gate on `[ -s ]`) | **Fixed 2026-09-14** (template defect surfaced by the second pass via the wrong Example 5 claim) | One line in each Python `run_agent`; L-1 duty met: `test_python_templates_treat_empty_success_as_failure` dry-runs both templates (empty node → exit 2; empty worker → abort); zero-sum kept — a provenance clause in the pack's Purpose that the Prompt Sources list already carries was removed; whole-file 19,969 → 19,952 chars (lint measures the whole file: headroom is 48 chars, not the "~700–800" body-based figure the September review and roadmap used — corrected there) | The lint-tier decision (GW-9) is now urgent, not comfortable |

## Evidence Actually Checked

- **Observed:** round-one router probes (15 phrases: 3 misses, of which 2 were the R11 gap; the other two were replaced without fixture-backing — corrected in round two); round-two pre-tune fixture numbers (0% / 80% / 0% / 75%) and post-tune evals at 100%; the second-pass routing lens's 12 fresh probes (4 misses on the round-one tree, all now fixture-backed); four template stub rigs (writer-critic 6 paths, multi-wave 6, orchestrator 6, DAG 8 — including the zero-byte case that exposed GW-15); `test_index_json_current.py` failing on a one-line wording change after strengthening; the near-duplicate prototype's output on both trees; `validate_skill_examples.py`; `make all` after the second pass.
- **Author-claimed:** none load-bearing.
- **[INFERENCE]:** the narrowed R11 context generalizes beyond the fixture phrases; guarded by 13 GD-19 probes, three adversarial traps, and the full holdout, not proven for unseen phrasing — round one's bare `verify` is the reminder that a passing fixture set does not bound the phrase space.
- **Not done:** the date-gated and host-only items above; no quick-cue bullet (R12 frequency bar not met).

## Second-Pass Review (2026-09-14)

User instruction: *"review again."* Three read-only lenses (routing tune; prompt text and examples; guards and records) over `b711499` and `b5c3a10`, plus the coordinator's execute slice (four template stub rigs). Verdict AGREE WITH CHANGES 3/3. Same-model lenses are one epistemic channel; the rigs and the router probes are the deterministic ones.

| # | Finding | Found by | Fix |
| --- | --- | --- | --- |
| S1 | Bare `verify` in the R11 context re-routed review / risk / test-plan requests about an approved spec into implement | routing lens (3 fresh regressions, reproduced) | `gate passes`; `approved_spec_verify_not_implement_trap`; contract bullet |
| S2 | Two failing GD-19 probe phrases replaced between runs instead of fixture-backed; one undisclosed | coordinator | both fixture-backed, pre-tune numbers recorded, tuned; disclosed in GW-1 |
| S3 | Catalog question "which skill should implement …" overridden by R11; selection targets lacked skill/pack; zh-TW delivery-plan marker missing; `money` not a risk keyword | routing lens (4 misses in 12 fresh probes) | catalog signal + R11 defers; targets; markers; keyword |
| S4 | My round-two trap phrases contained the literal pack name, which the GD-17 guard forbids in any fixture | coordinator | generic wording, per the P7 convention |
| S5 | GW-4 sentence carried repo-history narration | text lens | clause stripped |
| S6 | GW-5 compaction dropped five clauses while the record said "same clauses" | text lens | restored |
| S7 | GLOSSARY status map omitted `needs-qualification`; "pinned by its skill's guard" false for research and review | text lens | row corrected; pins added |
| S8 | Example 4 asserted worker confinement the template does not provide; Example 5 asserted a per-node output gate it lacked | text lens + DAG rig (zero-byte node marked `done`, observed) | examples corrected; template defect fixed as GW-15 |
| S9 | Index guard compared line counts only; same-length edits escaped | guards lens + coordinator | whole-entry and category comparison |
| S10 | TK-1 absence assertion checked a fragment that is not in the reserved sentence (vacuous) | guards lens | fragment from the reserved wording |
| S11 | "Seven adopted / seven held" was 7 / 1 rejected / 6 held; the September entry's 14 / 5 / 3 was 14 / 4 / 4 | guards lens | record and both Decision Index entries corrected |
| S12 | Flow-pack headroom stated body-based (~700–800 chars); lint measures the whole file: 31 and 73 chars at `b5c3a10` | coordinator (measured) | September review C15, roadmap, runbook corrected |
| S13 | Orchestrator template holds a literal ``` inside a string; a lazy fence regex truncates it | coordinator (guard authoring) | extraction anchored to a fence on its own line |

Confirmed sound by the lenses: every `b711499` merge preserved every unique clause; every RETIRED fragment is a real substring of the retired text; new fixture groups match sibling rigor/enhancements; fixture floors and test-count deltas verified to the test; `IndexGenerator.generate()` has no side effects.

## Falsifiability

This record is wrong if: a GD-19 probe or fixture group is removed while the guard passes; `index.json` goes stale without `test_index_json_current.py` failing; the search-before-add sentence or the status-family table disappears; a held item above is adopted without its named trigger firing; or a future near-duplicate detector is claimed to work without passing the GW-8 discriminating test.

## Completion Ledger

| Item | Status | Evidence |
| --- | --- | --- |
| GW-1 fixtures, tune, contract, floors, probes (two rounds) | done | ROUTE-001/002/003 100%; `test_validate_route_fixture.py`; `test_quality_gates_summary.py`; `test_governed_delivery_adoption_state.py` (no pack name in any fixture) |
| GW-2 guard (whole-entry) | done | fails stale / passes current (observed both, including a same-line-count edit) |
| GW-15 template fix + dry-run guard | done | `test_skill_verification_panel_record.py` 18 passed; pack under the lint threshold |
| Second-pass review (3 lenses, coordinator rigs) | done | §Second-Pass Review below |
| GW-3–GW-7 surfaces | done | named files |
| GD-19 ledger row and roadmap rows updated | done | GD adoption ledger; roadmap Horizon 3 |
| Decision Index, index.json | done | `PROJECT_KNOWLEDGE.md`; regenerated |
| Full repository gate | verified | `make all` from repository root |
